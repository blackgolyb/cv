\cvSkills
{\VAR{ data.skills|length }}
{
  \BLOCK{ for category in data.skills }
    \cvSkillsCategory{\VAR{ category.name }}{
      \BLOCK{ for skill in category.skills }
        \cvSkillsItem{\VAR{ skill }}{}
      \BLOCK{ endfor }
    }
  \BLOCK{ endfor }
}